Pre-study project with two prototypes (ll-hls, webrtc), measured server side and client side performance. The report focuses on server-side process CPU, memory, and RSA plus client-side latency and TTFF due to page limits but there are many more plots inside /plots directory. 
